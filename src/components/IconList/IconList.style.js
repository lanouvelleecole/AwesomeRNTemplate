import { StyleSheet } from "react-native";

/**
 * Ci-dessous, voici les styles à appliquer à ce IconList
 *
 */
const styles = StyleSheet.create({
  container: {
    backgroundColor: "red",
  },
  dataListContainer: {
    flex: 1,
  },
  whiteText: {
    color: "white",
  },
});

export { styles };
