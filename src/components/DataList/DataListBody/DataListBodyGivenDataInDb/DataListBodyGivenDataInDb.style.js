import { StyleSheet } from "react-native";

/**
 * Ci-dessous, voici les styles à appliquer à ce DataListBodyGivenDataInDb
 *
 */
const styles = StyleSheet.create({
  centeredContainer: {
    flex: 1,
    justifyContent: "center",
  },
  whiteText: {
    color: "white",
  },
});

export { styles };
