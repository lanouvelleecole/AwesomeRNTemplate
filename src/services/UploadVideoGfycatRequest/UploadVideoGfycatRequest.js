/* PLOP_INJECT_IMPORT */
//import React from "react";

import ReactNativeBlobUtil from 'react-native-blob-util';
import {RunIfPossible} from '../RunIfPossible/RunIfPossible';
import {MuxVideos} from '../MuxVideos/MuxVideos.js';

/**
 *
 * @param {*} param1,
 *
 * @returns ...
 *
 * Cette fonction permet de ...
 */

const UploadVideoGfycatRequest = async ({
  gfyName,
  filePath,
  accessToken,
  grabCanceller,
  onDone,
  onError,
}) => {
  MuxVideos;
};

export {UploadVideoGfycatRequest};
