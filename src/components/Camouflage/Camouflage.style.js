import { StyleSheet } from "react-native";

/**
 * Ci-dessous, voici les styles à appliquer à ce Camouflage
 *
 */
const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export { styles };
