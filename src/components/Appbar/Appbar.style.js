import { StyleSheet } from "react-native";

/**
 * Ci-dessous, voici les styles à appliquer à ce Appbar
 *
 */
const styles = StyleSheet.create({
  container: {
    backgroundColor: "red",
  },
  whiteText: {
    color: "white",
  },
});

export { styles };
